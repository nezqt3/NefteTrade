# OilTransport Frontend

Платформа для поиска перевозок нефтепродуктов - Frontend приложение

## 🚀 Технологии

- **React 18** + **TypeScript**
- **Vite** - быстрая сборка
- **Ant Design** - UI компоненты
- **React Router** - маршрутизация
- **TanStack Query** - управление данными
- **Zustand** - state management
- **Axios** - HTTP клиент
- **Socket.IO Client** - WebSocket для чата

## 📦 Установка

```bash
# Установка зависимостей
npm install

# Копирование файла окружения
cp .env.example .env

# Редактирование .env (укажите URL вашего backend API)
# VITE_API_URL=http://localhost:4000/api/v1
```

## 🏃 Запуск

```bash
# Режим разработки
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр production сборки
npm run preview
```

Приложение будет доступно по адресу: `http://localhost:3000`

## 📁 Структура проекта

```
src/
├── app/                # Инициализация приложения
│   ├── store/         # Глобальное состояние (Zustand)
│   ├── App.tsx        # Главный компонент
│   └── Router.tsx     # Роутинг
├── entities/          # Бизнес-сущности
│   ├── user/
│   ├── ad/
│   └── payment/
├── features/          # Фичи приложения
│   ├── auth/
│   ├── ads/
│   └── payment/
├── pages/             # Страницы
│   ├── HomePage/
│   ├── LoginPage/
│   ├── RegisterPage/
│   ├── AdsListPage/
│   └── AdDetailPage/
├── shared/            # Общие утилиты
│   ├── api/          # API клиент
│   ├── config/       # Конфигурация
│   └── ui/           # Переиспользуемые UI компоненты
└── widgets/           # Виджеты
    └── Header/
```

## 🎨 Дизайн

Приложение использует цветовую схему:

- **Primary**: `#0d94db` (синий)
- **Primary Light**: `#75c0e3`
- **Primary Dark**: `#4cabda`
- **Background**: `#e4eaec`
- **Text colors**: `#4c545c`, `#5e6669`, `#8f9698`

## ✨ Основные функции

### Для заказчиков (Customers)
- ✅ Регистрация и авторизация
- ✅ Создание заказов на перевозку
- ✅ Просмотр предложений от подрядчиков
- 🚧 Система рейтингов и отзывов
- 🚧 История заказов

### Для подрядчиков (Contractors)
- ✅ Регистрация с верификацией документов
- ✅ Поиск заказов с фильтрами
- ✅ Оплата доступа к контактам (500₽)
- ✅ Чат с заказчиками после оплаты
- 🚧 Статистика и аналитика

### Общие функции
- ✅ Адаптивный дизайн
- ✅ Поиск по маршруту, типу продукта, цене
- ✅ Infinite scroll для списка объявлений
- 🚧 Уведомления в реальном времени
- 🚧 Мультиязычность

## 🔐 Безопасность

- JWT аутентификация с refresh токенами
- HttpOnly cookies для токенов
- Автоматическое обновление токенов
- Защита роутов (ProtectedRoute)
- Скрытие контактов до оплаты

## 🔌 API Endpoints

Приложение взаимодействует со следующими endpoints:

```
POST   /api/v1/auth/login           # Вход
POST   /api/v1/auth/register        # Регистрация
POST   /api/v1/auth/logout          # Выход
GET    /api/v1/auth/me              # Текущий пользователь
POST   /api/v1/auth/refresh         # Обновление токена

GET    /api/v1/ads                  # Список объявлений
GET    /api/v1/ads/:id              # Объявление по ID
POST   /api/v1/ads                  # Создать объявление
PATCH  /api/v1/ads/:id              # Обновить объявление
DELETE /api/v1/ads/:id              # Удалить объявление

POST   /api/v1/payments             # Создать платеж
POST   /api/v1/payments/unlock/:id  # Открыть контакт
GET    /api/v1/payments/:id         # Статус платежа
```

## 🛠 Разработка

### Добавление новой страницы

1. Создайте компонент страницы в `src/pages/`
2. Добавьте роут в `src/app/Router.tsx`
3. При необходимости создайте API методы в `src/features/`

### Добавление новой сущности

1. Создайте типы в `src/entities/{entity}/model/types.ts`
2. Создайте UI компоненты в `src/entities/{entity}/ui/`
3. Создайте API методы в `src/features/{entity}/api/`

## 📝 TODO

- [ ] Страница создания объявления
- [ ] Страница профиля пользователя
- [ ] Страница "Мои объявления"
- [ ] Реальное время чат (WebSocket)
- [ ] Загрузка документов для верификации
- [ ] Система уведомлений
- [ ] Админ-панель
- [ ] Тесты (Jest + React Testing Library)
- [ ] E2E тесты (Playwright/Cypress)

## 🤝 Вклад в разработку

1. Fork репозиторий
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

MIT

## 👥 Контакты

Вопросы и предложения: support@oiltransport.ru
